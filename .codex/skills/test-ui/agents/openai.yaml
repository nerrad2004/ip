interface:
  display_name: "Test UI"
  short_description: "Run fail-fast chatbot console UI tests"
  default_prompt: "Use $test-ui to run the UI test plan and show the full console transcript."

policy:
  allow_implicit_invocation: true
